const TRANSPORT_TYPES = [
  `taxi`,
  `bus`,
  `train`,
  `ship`,
  `transport`,
  `drive`,
  `flight`,
];

const STOP_TYPES = [
  `check-in`,
  `sightseeing`,
  `restaurant`
];

export {TRANSPORT_TYPES, STOP_TYPES};
